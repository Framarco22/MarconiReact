import cart from './assets/cart.jpg'

const CartWidget = () => {
    return (
        <div>
            < img src={cart} alt="cart-widget" width={'50px'} />
            0
        </div>
    )
}

export default CartWidget

